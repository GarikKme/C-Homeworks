@model IEnumerable<User>

@{
    ViewData["Title"] = "User list";
}

<div class="container shadow p-5">
    <h1>User CRUD Demo</h1>

    <div class="col mb-3">
        <a asp-controller="User" asp-action="Create" class="btn btn-lg btn-primary"><i class="bi bi-file-plus-fill"></i>Add User</a>
    </div>

    <table class="table table-bordered table-hover">
        <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col">Age</th>
            <th scope="col">Address</th>
            <th scope="col">Created At</th>
            <th></th>
        </tr>
        </thead>
        <tbody>
        @foreach(var user in Model)
        {
            <tr>
                <td width="20%">@user.Name</td>
                <td width="20%">@user.Phone</td>
                <td width="20%">@user.WorkPhone</td>
                <td width="20%">@user.Email</td>
                <td width="25%">@user.Description</td>
                <td width="20%">@user.CreatedAt</td>
                <td>
                    <div role="group" class="w-60 btn-group">
                        <span style="margin-right: 5px;">
                            <a asp-controller="User" asp-action="Edit" asp-route-id="@user.Id" class="btn btn-sm btn-primary"><i class="bi bi-pencil-square"></i>Edit</a>
                        </span>
                        <span>
                            <a asp-controller="User" asp-action="Delete" asp-route-id="@user.Id" class="btn btn-sm btn-danger"><i class="bi bi-trash-fill"></i>Delete</a>
                        </span>
                    </div>
                </td>
            </tr>
        }
        </tbody>
    </table>
</div>