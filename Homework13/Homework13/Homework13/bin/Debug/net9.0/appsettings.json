{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "DefaultConnection": "server=127.0.0.1;port=3306;database=homework13;user=root;password="
  }
}

//1. В терминале в папке с проектом (где .csproj) выполни:
//dotnet new tool-manifest
//dotnet tool install dotnet-ef --version 9.0.0

//2. Создай миграцию:
//dotnet tool run dotnet-ef migrations add Initial
//3. Применить миграцию к базе:
//dotnet tool run dotnet-ef database update

//dotnet tool run dotnet-ef migrations add UserModel
//dotnet tool run dotnet-ef database update